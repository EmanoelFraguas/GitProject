program ex5;

var
n, x1, x2, y1, y2, u1, u2, v1, v2: integer;
begin
	readln(n);
	while n <> 0 do
	begin
		read(x1, y1, u1, v1);
		for i := 1 to n-1 do
		begin
			read(x2, y2, u2, v2);
			
		end;
	end;
end.